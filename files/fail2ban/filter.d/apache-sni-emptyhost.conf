[Definition]
# [ssl:error] ... AH02031: Hostname X provided via SNI, but no hostname provided in HTTP request
# use on: error_log
failregex = \[ssl:error\].*\[client <HOST>(?::\d+)?\]\s+AH02031:\s+Hostname .* provided via SNI, but no hostname provided in HTTP request
ignoreregex =
